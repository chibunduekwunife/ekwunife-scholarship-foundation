import ApplyButton from "@/app/(landing)/(pages)/scholarships/components/apply-button";
import PageFormat from "@/app/(landing)/(pages)/scholarships/components/page-format";
import { TitleStyledComponent } from "@/app/(landing)/(pages)/scholarships/components/styled-components";
import { SSCE_data } from "@/app/(landing)/data/scholarships-data";

export default function Step4() {
  return (
    <div>
        Program Checklist
    </div>
  );
}
